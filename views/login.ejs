<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Nhập | Web Bán Hàng</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>

    <div class="login-card">
        <a href="/" class="home-btn" title="Về trang chủ">
            <img src="/images/home-icon.png" alt="Home" style="width: 40px; height: 32px;">
        </a>

        <div class="login-header">
            <h2>Welcome Back</h2>
            <p>Vui lòng đăng nhập tài khoản của bạn</p>
        </div>

        <% if (error) { %>
            <div class="alert-error">⚠️ <%= error %></div>
        <% } %>

        <form action="/login" method="POST">
            <div class="input-group">
                <label>Tên đăng nhập</label>
                <input type="text" name="username" placeholder="Ví dụ: admin" required>
            </div>
            
            <div class="input-group">
                <label>Mật khẩu</label>
                <input type="password" name="password" placeholder="Nhập mật khẩu" required>
            </div>

            <button type="submit" class="btn-login">ĐĂNG NHẬP NGAY</button>
        </form>

        <div class="footer-link">
            Chưa có tài khoản? <a href="/register">Đăng ký mới</a>
        </div>
    </div>
</body>
</html>